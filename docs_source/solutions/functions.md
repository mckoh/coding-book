# Working with Functions

```python

# create a new function
def calculate_area(radius):
    return round(

        # calculate area
        ((2 * radius) ** 2 * 3.1415) / 4,

        # round result to 2 digits
        2
    )

# call the function and hand-over a radius value
calculate_area(5)
```